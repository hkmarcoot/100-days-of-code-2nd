const mergeSort = (startArray) => {
  const length = startArray.length;
  if (length === 1) {
    return startArray;
  }
};

const inputArr = [3];

console.log(mergeSort(inputArr));

module.exports = {
  mergeSort,
};
