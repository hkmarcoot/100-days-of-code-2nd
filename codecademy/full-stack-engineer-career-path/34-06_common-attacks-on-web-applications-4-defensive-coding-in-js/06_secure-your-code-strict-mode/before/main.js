// Add strict mode below

function printNames(names, names) {
  console.log(
    `The ${names.length > 1 ? "names are" : "name is"} ${names.join(", ")}.`
  );
}

const arguments = ["Amelia", "Ethan", "Ava", "Mia"];
printNames("Noah", arguments);

companyName = "Codecademy";
delete printNames;
